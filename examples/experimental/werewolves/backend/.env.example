# Server Configuration
PORT=8000

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,https://*.vercel.app

# OpenAI API Key
# If using conda env sotopia-rl, this should already be set as an environment variable
# If not, uncomment and add your key:
# OPENAI_API_KEY=sk-...

# Redis for Sotopia (should already be configured in your conda env)
# If not set in environment, uncomment:
# REDIS_OM_URL=redis://:@localhost:6379

# Redis for game state (for future use with Option B)
# REDIS_URL=redis://localhost:6379
